FROM node:22

WORKDIR /var/www/html

# In dev mode, the project is mounted so the container always sees current code.
# Automatically install dependencies and run the Vite dev server.
CMD ["sh", "-c", "npm install && npm run dev"]
